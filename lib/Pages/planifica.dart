import 'package:flutter/material.dart';

class Planifica extends StatelessWidget {
  const Planifica({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Planifica')),
      body: const Center(child: Text('Aquí va la planificación')),
    );
  }
}
