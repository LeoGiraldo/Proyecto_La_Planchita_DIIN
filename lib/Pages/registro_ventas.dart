import 'package:flutter/material.dart';

class RegistroVentas extends StatelessWidget {
  const RegistroVentas({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registro de Ventas')),
      body: const Center(child: Text('Aquí va el registro de ventas')),
    );
  }
}
